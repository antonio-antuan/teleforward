name: Slash Command Dispatch
on:
  issue_comment:
    types: [created]
jobs:
  slashCommandDispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Slash Command Dispatch
        uses: peter-evans/slash-command-dispatch@v3
        with:
          token: ${{ secrets.PAT }}
          commands: auth-code
          repository: ${{ github.repository }}
          issue-type: issue

      - name: Get auth code
        id: vars
        run: |
          code=${{ github.event.client_payload.slash_command.args.unnamed.arg1 }}
          echo "$branch"
